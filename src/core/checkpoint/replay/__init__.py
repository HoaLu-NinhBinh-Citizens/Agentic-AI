"""Replay module."""

from typing import Any


class Replay:
    """Replay from checkpoint."""
    
    async def replay(self, checkpoint_id: str) -> Any:
        """Replay from checkpoint."""
        return None
